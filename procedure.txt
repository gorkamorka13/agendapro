
npx create-next-app@latest agenda-app
cd agenda-app
npm install prisma --save-dev
npx prisma init
Get-Service -Name "postgres*"
npx prisma migrate reset
npm run dev
npx prisma migrate dev --name "create-full-project-schema"
npx prisma studio

Dans le fichier .env:
DATABASE_URL="postgresql://postgres:brigitte@localhost:5432/postgres?schema=public"

import { defineConfig, env } from "prisma/config";
import "dotenv/config";

Dans le fichier prisma.config.ts:
export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  engine: "classic",
  datasource: {
    url: env("DATABASE_URL"),
  },
});

 npm install bcrypt
 npm install next-auth
npm install --save-dev @types/bcrypt
npx prisma db seed
npm install @next-auth/prisma-adapter
